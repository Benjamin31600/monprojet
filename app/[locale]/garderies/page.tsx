import type { Metadata } from "next";
import Link from "next/link";
import { getChildcareData, matchReasons, normalize, rankChildcare, typeLabel } from "@/lib/childcare";

export const dynamic = "force-dynamic";

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> {
  const { locale } = await params;
  const fr = locale !== "en";
  return {
    title: fr ? "Services de garde au Québec | MyCoco" : "Childcare services in Quebec | MyCoco",
    description: fr ? "Découvrez et comparez les services de garde selon votre secteur, l'âge de votre enfant et votre besoin." : "Discover and compare childcare services by area, child age and need.",
  };
}

export default async function GarderiesPage({ params, searchParams }: { params: Promise<{ locale: string }>; searchParams: Promise<Record<string, string | string[] | undefined>> }) {
  const { locale } = await params;
  const q = await searchParams;
  const fr = locale !== "en";
  const data = await getChildcareData();
  const query = typeof q.ville === "string" ? q.ville : "";
  const type = typeof q.type === "string" ? q.type : "";
  const age = typeof q.age === "string" ? q.age : "";
  const debut = typeof q.debut === "string" ? q.debut : "";
  const demandId = typeof q.demandeId === "string" ? q.demandeId : "";
  const n = normalize(query);
  const filtered = data.records.filter((r) => {
    if (!n) return true;
    const c = normalize(r.city);
    const p = normalize(r.postalCode).replace(/\s+/g, "");
    const needle = n.replace(/\s+/g, "");
    return c.includes(n) || p.includes(needle);
  });
  const ranked = rankChildcare(filtered, query, type, undefined, undefined, age);
  const hasNeed = Boolean(query || type || age || debut);

  return <main className="mc-directory-v9">
    <section className="mc-dir-hero"><div className="mc-dir-wrap"><div className="mc-dir-crumb"><Link href={`/${locale}`}>MyCoco</Link><span>›</span><span>{fr ? "Services de garde" : "Childcare"}</span></div><div className="mc-dir-title"><div><span>{fr ? "DÉCOUVRIR LES SERVICES" : "DISCOVER PROVIDERS"}</span><h1>{fr ? "Comparez d'abord. Contactez ensuite." : "Compare first. Contact second."}</h1><p>{fr ? "Consultez les informations réellement disponibles pour chaque service. Les photos, avis et badges n'apparaissent que lorsqu'ils sont vérifiables sur MyCoco." : "Review information actually available for each provider. Photos, reviews and badges only appear when they are verifiable on MyCoco."}</p></div><Link className="mc-dir-maincta" href={`/${locale}/mon-besoin`}>{fr ? "Créer ma recherche" : "Create my search"} →</Link></div></div></section>
    <section className="mc-dir-body"><div className="mc-dir-wrap">
      <form className="mc-dir-filters" method="get"><label><span>{fr ? "Où ?" : "Where?"}</span><input name="ville" defaultValue={query} placeholder={fr ? "Ville ou code postal" : "City or postal code"}/></label><label><span>{fr ? "Âge" : "Age"}</span><select name="age" defaultValue={age}><option value="">{fr ? "Tous" : "All"}</option><option value="0-18">0–18 {fr ? "mois" : "months"}</option><option value="18-36">18–36 {fr ? "mois" : "months"}</option><option value="3-5">3–5 {fr ? "ans" : "years"}</option><option value="5+">5+ {fr ? "ans" : "years"}</option></select></label><label><span>{fr ? "Type" : "Type"}</span><select name="type" defaultValue={type}><option value="">{fr ? "Tous les services" : "All providers"}</option><option value="CPE">CPE</option><option value="subventionnee">{fr ? "Garderie subventionnée" : "Subsidized daycare"}</option><option value="milieu familial">{fr ? "Milieu familial" : "Home childcare"}</option><option value="non subventionnee">{fr ? "Garderie non subventionnée" : "Non-subsidized daycare"}</option></select></label><button>{fr ? "Rechercher" : "Search"} →</button></form>
      {hasNeed && <div className="mc-dir-active"><b>{fr ? "Recherche active" : "Active search"}</b><span>{query || (fr ? "Tous secteurs" : "All areas")}</span>{age && <span>{age}</span>}{type && <span>{typeLabel(type, fr)}</span>}<Link href={`/${locale}/mon-besoin`}>{fr ? "Modifier" : "Edit"}</Link></div>}
      <div className="mc-dir-count"><div><span>{fr ? "RÉSULTATS" : "RESULTS"}</span><h2>{ranked.length} {fr ? "services trouvés" : "providers found"}</h2></div><p>{fr ? "Aucun avis ni aucune photo n'est inventé par MyCoco." : "MyCoco never invents reviews or provider photos."}</p></div>
      {ranked.length === 0 ? <div className="mc-dir-empty"><h3>{fr ? "Aucun service pour ces critères." : "No provider matches these filters."}</h3><Link href={`/${locale}/mon-besoin`}>{fr ? "Élargir ma recherche" : "Broaden my search"} →</Link></div> : <div className="mc-dir-grid">{ranked.slice(0, 30).map((record) => { const reasons = matchReasons(record, query, type, fr, age); let href = `/${locale}/garderie/${record.slug}`; if (demandId) href += `?demandeId=${encodeURIComponent(demandId)}`; return <Link className="mc-dir-card" key={record.id} href={href}><div className="mc-dir-photo-placeholder" aria-label={fr ? "Aucune photo vérifiée pour le moment" : "No verified photo yet"}><div><span>mycoco</span><b>{record.name.slice(0, 1).toUpperCase()}</b><small>{fr ? "Photo du service à venir" : "Provider photo coming soon"}</small></div><em>{typeLabel(record.type, fr)}</em></div><div className="mc-dir-card-body"><div className="mc-dir-card-top"><span>{record.city}{record.postalCode ? ` · ${record.postalCode}` : ""}</span><span className="mc-dir-no-rating">☆ {fr ? "Aucun avis publié" : "No published reviews"}</span></div><h3>{record.name}</h3><div className="mc-dir-reasons">{reasons.slice(0, 3).map((reason) => <span key={reason}>✓ {reason}</span>)}</div><div className="mc-dir-foot"><small>{fr ? "Informations issues des données disponibles" : "Based on available data"}</small><b>{fr ? "Voir la fiche" : "View profile"} →</b></div></div></Link>; })}</div>}
      <div className="mc-dir-trust"><span>i</span><p><b>{fr ? "Transparence MyCoco" : "MyCoco transparency"}</b>{fr ? ` Les résultats aident à découvrir et comparer. Une place doit toujours être confirmée auprès du service et, lorsque requis, via les démarches officielles du Québec. Données mises à jour : ${data.updatedAt || "—"}.` : ` Results help with discovery and comparison. A spot must always be confirmed with the provider and, when required, through Quebec's official process. Data updated: ${data.updatedAt || "—"}.`}</p></div>
    </div></section>
    <style>{`.mc-directory-v9{background:#f8f5ef;color:#292521;min-height:100vh}.mc-dir-wrap{width:min(1180px,calc(100% - 40px));margin:auto}.mc-dir-hero{padding:34px 0 58px;background:#efe6d9;border-bottom:1px solid #ded4c7}.mc-dir-crumb{display:flex;gap:8px;color:#7e756b;font-size:10px;margin-bottom:42px}.mc-dir-title{display:grid;grid-template-columns:1fr auto;gap:40px;align-items:end}.mc-dir-title>div>span,.mc-dir-count span{font-size:10px;font-weight:950;letter-spacing:.15em;color:#9a6247}.mc-dir-title h1{font-size:clamp(48px,6vw,76px);line-height:.94;letter-spacing:-.07em;max-width:800px;margin:12px 0 17px}.mc-dir-title p{max-width:690px;color:#6e675f;font-size:15px;line-height:1.65;margin:0}.mc-dir-maincta{background:#1d5144;color:#fff;padding:15px 18px;border-radius:12px;font-size:11px;font-weight:900;white-space:nowrap}.mc-dir-body{padding:28px 0 80px}.mc-dir-filters{display:grid;grid-template-columns:1.2fr .8fr 1fr auto;gap:8px;background:#fff;border:1px solid #e1dbd1;border-radius:17px;padding:9px}.mc-dir-filters label{display:grid;gap:5px;padding:4px 8px}.mc-dir-filters label span{font-size:8px;font-weight:900;color:#797169}.mc-dir-filters input,.mc-dir-filters select{height:36px;border:0;background:transparent;color:#292521;outline:none;font-size:11px}.mc-dir-filters button{border:0;border-radius:11px;background:#292521;color:#fff;padding:0 18px;font-size:10px;font-weight:900}.mc-dir-active{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:12px;padding:10px 12px;background:#edf4f0;border-radius:12px;font-size:9px}.mc-dir-active b{color:#1d5144}.mc-dir-active span{background:#fff;padding:6px 8px;border-radius:999px}.mc-dir-active a{margin-left:auto;font-weight:900}.mc-dir-count{display:flex;justify-content:space-between;gap:25px;align-items:end;margin:46px 0 18px}.mc-dir-count h2{font-size:32px;letter-spacing:-.05em;margin:6px 0 0}.mc-dir-count p{color:#817970;font-size:9px}.mc-dir-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.mc-dir-card{background:#fff;border:1px solid #e1dbd1;border-radius:20px;overflow:hidden;transition:transform .15s ease,box-shadow .15s ease}.mc-dir-card:hover{transform:translateY(-3px);box-shadow:0 16px 38px rgba(41,37,33,.1)}.mc-dir-photo-placeholder{height:190px;background:linear-gradient(145deg,#dfeae4,#f3ebe1);padding:15px;position:relative}.mc-dir-photo-placeholder>div{height:100%;display:grid;place-items:center;align-content:center;gap:6px;border:1px dashed rgba(41,37,33,.15);border-radius:13px}.mc-dir-photo-placeholder>div span{font-size:9px;font-weight:950;letter-spacing:.12em;color:#657a70}.mc-dir-photo-placeholder>div b{font-size:38px;color:#315d50}.mc-dir-photo-placeholder>div small{font-size:8px;color:#7b756e}.mc-dir-photo-placeholder>em{position:absolute;left:24px;top:24px;background:#fff;padding:6px 8px;border-radius:999px;font-size:8px;font-weight:900;font-style:normal}.mc-dir-card-body{padding:18px}.mc-dir-card-top{display:flex;justify-content:space-between;gap:10px;color:#837b72;font-size:8px}.mc-dir-no-rating{white-space:nowrap}.mc-dir-card h3{font-size:21px;line-height:1.05;letter-spacing:-.04em;margin:10px 0 12px}.mc-dir-reasons{display:flex;flex-wrap:wrap;gap:5px;min-height:24px}.mc-dir-reasons span{padding:5px 7px;border-radius:999px;background:#eef4f0;color:#3f6659;font-size:7px;font-weight:800}.mc-dir-foot{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-top:18px;padding-top:13px;border-top:1px solid #eee9e2}.mc-dir-foot small{color:#918980;font-size:7px}.mc-dir-foot b{font-size:9px}.mc-dir-empty{padding:50px;background:#fff;border-radius:20px;text-align:center}.mc-dir-trust{display:flex;gap:10px;margin-top:30px;padding:16px;background:#fff;border:1px solid #e1dbd1;border-radius:14px}.mc-dir-trust>span{display:grid;place-items:center;width:24px;height:24px;flex:0 0 24px;border-radius:50%;background:#1d5144;color:#fff;font-size:10px}.mc-dir-trust p{margin:0;color:#746d66;font-size:9px;line-height:1.6}.mc-dir-trust b{color:#292521}@media(max-width:900px){.mc-dir-grid{grid-template-columns:1fr 1fr}.mc-dir-title{grid-template-columns:1fr}.mc-dir-filters{grid-template-columns:1fr 1fr}.mc-dir-filters button{min-height:44px}}@media(max-width:600px){.mc-dir-wrap{width:calc(100% - 28px)}.mc-dir-grid,.mc-dir-filters{grid-template-columns:1fr}.mc-dir-title h1{font-size:46px}.mc-dir-count{align-items:start;flex-direction:column}.mc-dir-photo-placeholder{height:175px}}`}</style>
  </main>;
}
