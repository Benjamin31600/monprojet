import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { isLocale, type Locale } from "@/lib/i18n";
import AuthPortal from "@/components/AuthPortal";

export const metadata: Metadata = { robots: { index: false, follow: false } };

export default async function SignupPage({ params, searchParams }: { params: Promise<{ locale: string }>; searchParams: Promise<Record<string, string | string[] | undefined>> }) {
  const { locale: raw } = await params;
  const q = await searchParams;
  if (!isLocale(raw)) notFound();
  const locale = raw as Locale;
  const fr = locale === "fr";
  const audience = q.role === "provider" || q.type === "provider" ? "provider" : "family";
  return <main className="mcs-auth-page"><section className="mcs-auth-shell"><div className="mcs-auth-copy">
    <Link href={`/${locale}`}>← {fr ? "Retour à MyCoco" : "Back to MyCoco"}</Link>
    <span>{audience === "family" ? (fr ? "ESPACE FAMILLE" : "FAMILY SPACE") : (fr ? "ESPACE SERVICE DE GARDE" : "CHILDCARE PROVIDER SPACE")}</span>
    <h1>{audience === "family" ? (fr ? "Enregistrez ce qui compte. Pas toute votre vie." : "Save what matters. Not your whole life.") : (fr ? "Prenez le contrôle de votre présence MyCoco." : "Take control of your MyCoco presence.")}</h1>
    <p>{audience === "family" ? (fr ? "Votre espace sert à retrouver vos recherches, vos favoris et vos futures alertes. Pour le matching initial, MyCoco ne demande pas le nom complet ni la date de naissance de votre enfant." : "Your space keeps searches, favourites and future alerts together. MyCoco does not need your child's full name or exact birth date for initial matching.") : (fr ? "Créez votre accès sécurisé, puis complétez votre fiche professionnelle avec les informations que vous choisissez de rendre publiques. Votre compte privé et votre fiche publique restent distincts." : "Create secure access, then complete your professional profile with the information you choose to make public. Your private account and public listing stay separate.")}</p>
    <div className="mcs-auth-points">{(audience === "family" ? (fr ? ["Recherche enregistrée","Favoris et suivi","Alertes à venir"] : ["Saved searches","Favourites and tracking","Future alerts"]) : (fr ? ["Fiche professionnelle","Informations sous votre contrôle","Demandes et outils à venir"] : ["Professional listing","Information under your control","Future demand and tools"])).map(item=><span key={item}>{item}</span>)}</div>
    <div className="mcs-auth-switch"><b>{fr ? "Vous n’êtes pas au bon endroit ?" : "Wrong space?"}</b><Link href={`/${locale}/inscription?role=${audience === "family" ? "provider" : "family"}`}>{audience === "family" ? (fr ? "Créer un espace service" : "Create provider space") : (fr ? "Créer un espace famille" : "Create family space")} →</Link></div>
  </div><AuthPortal locale={locale} audience={audience} mode="signup" /></section>
  <style>{`
    .mcs-auth-page{--blue:#333873;--blue-dark:#242954;--yellow:#ffd44f;--bg:#f7f7fb;--line:#e1e2ea;--muted:#737587;min-height:calc(100vh - 72px);background:var(--bg);color:#2b2d42;padding:52px 20px 76px;font-family:Arial,Helvetica,sans-serif}.mcs-auth-shell{width:min(1080px,100%);margin:auto;display:grid;grid-template-columns:minmax(0,1fr) 430px;gap:72px;align-items:center}.mcs-auth-copy>a{display:inline-flex;margin-bottom:52px;color:#77798a;font-size:10px;font-weight:850}.mcs-auth-copy>span{font-size:10px;font-weight:900;letter-spacing:.1em;color:#858797}.mcs-auth-copy h1{max-width:650px;margin:12px 0 17px;color:var(--blue);font-size:clamp(43px,5.8vw,68px);line-height:.98;letter-spacing:-.055em}.mcs-auth-copy>p{max-width:600px;margin:0;color:var(--muted);font-size:15px;line-height:1.65}.mcs-auth-points{display:flex;gap:7px;flex-wrap:wrap;margin-top:22px}.mcs-auth-points span{padding:7px 9px;border:1px solid #d9dae4;border-radius:999px;background:#fff;color:#666879;font-size:9px;font-weight:800}.mcs-auth-switch{margin-top:27px;padding-top:18px;border-top:1px solid #dfe0e7;display:grid;gap:4px;max-width:450px}.mcs-auth-switch b{color:#555768;font-size:10px}.mcs-auth-switch a{color:var(--blue);font-size:10px;font-weight:900}
    .mcs-auth-page .mc-auth13{border-color:#dedfe8;border-radius:16px;box-shadow:0 18px 50px rgba(51,56,115,.09);color:#2b2d42}.mcs-auth-page .mc-auth13-role>span{border-radius:50%;background:#f0edff;color:var(--blue)}.mcs-auth-page .mc-auth13.provider .mc-auth13-role>span{background:var(--yellow);color:var(--blue-dark)}.mcs-auth-page .mc-auth13-role small,.mcs-auth-page .mc-auth13-role p{color:#858797}.mcs-auth-page .mc-auth13-role b{color:var(--blue)}.mcs-auth-page .mc-auth13-tabs{background:#f4f4f8}.mcs-auth-page .mc-auth13-tabs button{color:#7a7c8c}.mcs-auth-page .mc-auth13-tabs button.active{color:var(--blue)}.mcs-auth-page .mc-auth13-free{background:#fff7d7}.mcs-auth-page .mc-auth13-free>span{background:var(--yellow);color:var(--blue-dark)}.mcs-auth-page .mc-auth13 label>span{color:#5e6070}.mcs-auth-page .mc-auth13 input,.mcs-auth-page .mc-auth13 select{border-color:#d8d9e3;border-radius:8px;color:#2b2d42}.mcs-auth-page .mc-auth13 input:focus,.mcs-auth-page .mc-auth13 select:focus{border-color:var(--blue);box-shadow:0 0 0 3px rgba(51,56,115,.08)}.mcs-auth-page .mc-auth13-submit,.mcs-auth-page .mc-auth13-next{border-radius:8px;background:var(--yellow);color:var(--blue-dark);box-shadow:none}.mcs-auth-page .mc-auth13-submit:hover,.mcs-auth-page .mc-auth13-next:hover{background:#f1c231}.mcs-auth-page .mc-auth13-progress span.on,.mcs-auth-page .mc-password-meter i.on{background:var(--blue)}.mcs-auth-page .mc-auth13-security{background:#f5f5fa;border-color:#e2e2ea}.mcs-auth-page .mc-auth13-security>span{color:var(--blue)}.mcs-auth-page .mc-auth13 a,.mcs-auth-page .mc-auth13-bottom button{color:var(--blue)}
    @media(max-width:850px){.mcs-auth-shell{grid-template-columns:1fr;gap:34px}.mcs-auth-copy>a{margin-bottom:30px}.mcs-auth-page{padding-top:30px}}@media(max-width:520px){.mcs-auth-page{padding-left:14px;padding-right:14px}.mcs-auth-copy h1{font-size:42px}}
  `}</style></main>;
}
